section .data
	msg = "Hello, World!\n\0"

section .code
	mov		fr,		1	    ; syscall code: printstr
	push  	msg			    ; pointer to the string
	pop 	arg1		    ; Load pointer into argument register
	mov		arg2,	0       ; string size (0 = print as c-string, all chars until '\0')
	vmcall
	exit